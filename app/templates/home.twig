{% extends "layout.twig" %}

{% block title %}Welcome to EcoRide{% endblock %}

{% block content %}
<section class="text-center mt-6 px-4">
    <h2 class="text-2xl font-bold text-green-700 mb-4">
        Where would you like to share a ride today?
    </h2>

    {% if user %}
    <p class="text-gray-700 mb-6 text-md">Welcome back, <strong>{{ user.name }}</strong>! Ready for your next eco-ride?</p>
    {% else %}
    <p class="text-gray-600 mb-6">Join us to reduce your carbon footprint while traveling smartly and affordably.</p>
    {% endif %}

    <a href="/request-ride" class="inline-block bg-green-600 text-white text-lg font-semibold px-6 py-3 rounded-lg shadow hover:bg-green-700 transition">
        Start a Carpool
    </a>
</section>

<section class="mt-10 px-6 max-w-xl mx-auto">
    <div class="bg-white rounded-xl shadow p-6">
        <h3 class="text-lg font-semibold text-green-800 mb-2">Why EcoRide?</h3>
        <ul class="text-gray-700 list-disc list-inside space-y-2">
            <li>Save on travel costs with trusted drivers</li>
            <li>Eco-friendly transport options</li>
            <li>Simple and fast ride requests</li>
            <li>Mobile-first, intuitive experience</li>
        </ul>
    </div>
</section>
{% endblock %}
